{
  "name": "@ansible/content-model",
  "version": "0.1.0",
  "description": "Universal Ansible automation content object model — types and schema only, no runtime dependencies. Shared contract between Portal content plugins, the compatibility shim and the MCP endpoint.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "license": "Apache-2.0",
  "publishConfig": {
    "access": "public",
    "main": "dist/index.cjs.js",
    "types": "dist/index.d.ts"
  },
  "backstage": {
    "role": "common-library"
  },
  "scripts": {
    "build": "backstage-cli package build",
    "lint": "backstage-cli package lint",
    "test": "backstage-cli package test",
    "clean": "backstage-cli package clean"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@backstage/cli": "^0.36.0"
  }
}
